// Implement the AuthService.
// It should have a `validateAdmin` method that checks a username and password against a hardcoded or database-stored admin account.
// It should have a `login` method that generates a JWT (JSON Web Token) for the authenticated user.
